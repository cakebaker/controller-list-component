A simple CakePHP component which returns a list of controllers plus the corresponding action names.

Usage: Add "ControllerList" to the $components array of your controller and call $this->ControllerList->getList() in your action.
